<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sanetna.estabina.demo.gozor">

    <application
        android:allowBackup="true"
        android:icon="@drawable/show"
        android:label="@string/app_name"
        android:roundIcon="@drawable/show"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".splach">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
        <activity android:name=".item" />
        <activity android:name=".RecycleItems" />
        <activity android:name=".descActivity" />
        <activity android:name=".comp_desc"></activity>
    </application>

</manifest>